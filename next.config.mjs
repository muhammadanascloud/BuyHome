/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Keep this for stricter checks in development.
    swcMinify: true, // Ensure SWC minification is enabled for performance.
  };
  
  export default nextConfig;
  